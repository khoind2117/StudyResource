@using Microsoft.AspNetCore.Identity
@using StudyResource.ViewModels.Account
@model LoginViewModel
@inject SignInManager<User> signInManager

@{
    ViewData["Title"] = "Đăng nhập";
    Layout = "_Layout";
}

<div class="container d-flex align-items-center justify-content-center" style="height: 100vh;">
    <div class="card shadow-lg p-4 login_section" style="max-width: 400px; width: 100%;">
        <form asp-controller="Account" asp-action="Login" asp-route-returnurl="@Model.ReturnUrl" method="post" role="form">
            <h1 class="text-center mb-4">Đăng nhập</h1>
            <hr class="mb-4" />
            @if (!ViewData.ModelState.IsValid)
            {
                <div asp-validation-summary="All" class="alert alert-danger" role="alert"></div>
            }
            <div class="mb-3">
                <label asp-for="Email" class="form-label fw-bold">Email</label>
                <input asp-for="Email" class="form-control" placeholder="Nhập email">
                <span asp-validation-for="Email" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Password" class="form-label fw-bold">Mật khẩu</label>
                <input asp-for="Password" type="password" class="form-control" placeholder="Nhập mật khẩu">
                <span asp-validation-for="Password" class="text-danger small"></span>
            </div>

            <div class="d-flex justify-content-center">
                <button type="submit" class="btn btn-primary">Đăng nhập</button>
            </div>

            <p class="text-center mt-3 mb-0">
                Bạn chưa có tài khoản? <a asp-action="Register" class="text-primary">Tạo tài khoản ở đây</a>
            </p>
        </form>
    </div>
</div>
